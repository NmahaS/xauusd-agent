// Git-sync DISABLED — was committing every 15 min causing 19k+ commits and Railway disk issues.
// Plans are persisted on Railway filesystem; GitHub sync is unnecessary for trading.

export async function syncFileToGithub(_filePath, _content) {
  return;
}
